[tools]
helm = "3"
jq = "1.7"

[tasks.setup]
description = "Verify the project CLIs"
run = "helm version --short && jq --version"

[tasks.check]
description = "Check shell syntax"
run = "bash -n list-helm-images.sh"
