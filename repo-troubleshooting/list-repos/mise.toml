[tools]
python = "3.10"
uv = "0.12.5"

[tasks.setup]
description = "Install locked development dependencies"
run = "uv sync --frozen"

[tasks.check]
description = "Run Python lint and type checks"
run = "uv run ruff check list-repos.py && uv run pyright list-repos.py"
